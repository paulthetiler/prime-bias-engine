# Summary Status UI contract

Summary cards render a labelled `STATUS` row using `results.status` from the canonical Prime Bias engine. The Summary must not calculate or reinterpret Status independently.
